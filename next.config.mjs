/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "images.unsplash.com",
      "i0.shbdn.com",
      "www.renault.com.tr",
    ],
  },
};

export default nextConfig;
